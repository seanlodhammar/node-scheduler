import { ScheduleObj } from "../types/schedule";
import { StructureItem } from "../types/general";

class Schedule {
    private schedule : ScheduleObj | undefined;
    constructor() {
        
    }
}